<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tugas.pelitabangsa.herusantoso.pelitabangsahospital">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/periksa"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".DokterAddActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".PasienAddActivity" />
        <activity android:name=".ObatAddActivity" />
        <activity android:name=".DokterUpdateActivity" />
        <activity android:name=".PasienUpdateActivity" />
        <activity android:name=".ObatUpdateActivity" />
        <activity android:name=".PeriksaAddActivity" />
        <activity android:name=".PeriksaUpdateActivity" />
        <activity android:name=".ResepAddActivity" />
        <activity android:name=".LoginActivity">
        </activity>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>